// Backwards compatibility for contracts written pre-KSP 1.1 where there was
// a definition for a Wheel PartModuleType.  Only include ModuleWheelMotor so
// that landing gear doesn't count as wheels.
@Contracts
{
    @MODULE_DEFINITIONS
    {
        Wheel = ModuleWheelBrakes
    }
}
