CONTRACT_TYPE:NEEDS[SCANsat]
{
	// Created by tattagreis

    name = OrbitPeriodTest

    // Contract text
    title = Orbit Period Test
    description = Orbit around Kerbin
    synopsis = Reach a fitting orbit
    completedMessage = HQ will be statisfied!

    // Controls for whether a contract can be declined or cancelled, default is true for both
    cancellable = true
    declinable = true

    // Target Celestial body - controls where the contract must be completed, has some automated
    // effects on numeric parameters (increasing science, etc.)
    targetBody = Kerbin

    // Contract rewards. Rewards will be multiplyed by targetBody difficulty value.
    rewardScience = 25.0
	rewardReputation = 40.0
    rewardFunds = 20000.0
	
	// Can only be done once
	maxCompletions = 1
	maxSimultaneous = 1
	
	// Trivial, Significant, Exceptional	
	prestige = Significant
	
	PARAMETER
	{
		name = OrbitPeriod1
		type = OrbitPeriod
		
		minPeriod = 6h 0m 50s
		maxPeriod = 6h 0m 52s
	}
}