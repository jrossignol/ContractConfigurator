// Contract for parameter test
CONTRACT_TYPE
{
    // Unique name of the contract (required)
    name = TestParameter_Kerbal

    group = ParameterTest
    maxSimultaneous = 1
    targetBody = Kerbin

    // Contract text
    title = Kerbal Parameters
    description = description
    synopsis = synopsis
    completedMessage = completedMessage

    PARAMETER
    {
        type = HasAstronaut

        trait = Pilot
        minExperience = 1
        maxExperience = 2
        minCount = 2
        maxCount = 10
    }

    PARAMETER
    {
        type = HasAstronaut

        trait = Scientist
        maxExperience = 2
        maxCount = 10
    }

    PARAMETER
    {
        type = HasAstronaut

        trait = Engineer
        minCount = 5
    }

    PARAMETER
    {
        type = HasAstronaut

        minExperience = 1
    }

    PARAMETER
    {
        type = RecoverKerbal

        kerbal = Jebediah Kerman
    }
}
