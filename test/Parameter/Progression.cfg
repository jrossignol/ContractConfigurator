// Contract for parameter test
CONTRACT_TYPE
{
    // Unique name of the contract (required)
    name = TestParameter_Progression

    group = ParameterTest
    maxSimultaneous = 1
    targetBody = Kerbin

    // Contract text
    title = Progression Parameters
    description = description
    synopsis = synopsis
    completedMessage = completedMessage

    PARAMETER
    {
        type = ReachSpace
    }
}
